2026-03-23 00:15 关键记忆
1. 主人确认唯一账号是 3228996679。3381065978 可以互动但不接受指令。
2. QQ 渠道必须纯文本，不能使用 Markdown 格式符号。
3. gnome-todo 的 todo.py 已重写，周二晚上可解析为 2026-03-24 20:00。
4. 正在继续排查晚安提醒重复发送问题。
2026-03-23 偏好更新
主人要求每天在早安和晚安汇报中加入 Copilot 额度信息（Premium 和 Chat 剩余百分比）。已更新两个 OpenClaw cron payload。
2026-03-23 13:44 新增长期偏好与事实
1. QQ 回复风格：尽可能简短，日常不超过 50 字，最多不超过 300 字；结果导向，非必要不展开技术细节。
2. 任务执行前沟通规则：先确认“已收到任务要求”，并给出预计耗时；默认耗时单位为“分钟”。
3. 主人要求少提反问：类似“这种事你自己能决定，不要问我”的任务，优先自主决策执行。
4. 手机日历能力边界：已确认 Redmi K40 节点可读 calendar.events，但 Android 节点未放行 calendar.add（网关报错 not in allowlist），当前无法直接远程直写新增 event。
5. 可行替代：通过 .ics 导入或 Google Calendar 渲染链接添加事件；已生成并发布过测试 .ics 到个人站点。

2026-03-23 15:22 操作记录与长期偏好追加

1) Skills 更改
- 已将 active self-improving skill 切换为 self-improving-agent（workspace/skills/self-improving-agent）。
- 已从磁盘卸载：workspace/skills/clawdhub、workspace/skills/self-improving、workspace/skills/xiucheng-self-improving-agent。

2) Self-improving 配置
- 已在 workspace 中为 self-improving-agent 初始化 .learnings 目录：~/.openclaw/workspace/.learnings/，包含 LEARNINGS.md、ERRORS.md、FEATURE_REQUESTS.md、heartbeat-state.md、README.md。
- 未将 .learnings/ 加入 .gitignore（按主人选择）。
- 已启用自我改进 hook，hook 文件位置：~/.openclaw/hooks/self-improvement（已执行 openclaw hooks enable self-improvement）。

3) 周日历与上课提醒
- 已实现脚本 scripts/weekly_calendar.sh，用于每周一 00:00 读取手机日历并生成本周课表，文件存放：~/.openclaw/workspace/schedules/weekly_20260323.md。
- 已为本周课程创建并注册约 16 条提醒（每节课前 20 分钟，发送到 Napcat/QQ 3228996679），cron 任务名以 reminder: 开头并设为一次性（运行后删除）。
- 已创建 cron job weekly-calendar-sync（周一 00:00 自动运行 scripts/weekly_calendar.sh）。

4) 心跳压缩（context compaction）策略更新
- heartbeat-compact-check 已调整为每 15 分钟运行（新 job id: c9421d8e-586d-4f45-a5c3-913885253063）。
- 新行为：只有在上下文占用 >=85% 且距上次通知 >= 6 小时时，才会向主人发送告警并发送 /compact；否则仅在本地记录（.learnings/compact_state.json 与 .learnings/LEARNINGS.md）。
- 已写入 ~/.openclaw/workspace/.learnings/compact_state.json（last_notified: null, cooldown_hours:6），并更新 heartbeat-state.md 的 last_run。

5) 主人偏好（新增，可长期记忆）
- 不要在每次完成任务后询问不必要的问题；不必要的后续提问默认省略（已记录并将在今后任务执行时遵循）。
- 保持 .learnings/ 为本地记录目录且不自动加入版本控制（依主人选择）。

6) 其它
- 已立即执行一次自我改进心跳扫描并记录结果（无待处理条目）。
- 使用手机日历读取事件（Redmi K40 via Tailscale）以构建课程表；注意 Android 节点不支持 calendar.add（已记录在 MEMORY.md）。

Logged: 2026-03-23T15:22:00+08:00
Source: session_actions
Tags: skills, self-improving, calendar, cron, compaction, preference


2026-03-23T22:04+08:00 新增自动化与偏好更新

1) 新增腳本
- 路徑：scripts/daily_hf_papers.py
- 功能概述：從 https://huggingface.co/papers/date/YYYY-MM-DD 抓取當天論文頁（解析內嵌 data-props JSON），按熱度指標（upvotes*100 + comments*5）選取前 5 篇，為每篇生成 3 句中文摘要（主要問題 / 核心方法 / 有趣發現），輸出纯文本並保存到 ~/.openclaw/workspace/schedules/hf_daily_{date}.txt；支持参数 --date、--json，遇到抓取或解析失败会输出降級可读报告而不崩溃。

2) 已测试与提交
- 本地验证命令：python3 /home/nansea/.openclaw/workspace/scripts/daily_hf_papers.py --date 2026-03-23
- 结果：成功生成报告并保存为 ~/.openclaw/workspace/schedules/hf_daily_2026-03-23.txt，亦支持 --json 输出结构化数据。
- 已提交到 git：commit aa5a4a8 "Add robust daily Hugging Face papers report script"。

3) 新建 cron 任务
- 名称：HF热点论文早报
- Job ID：47bf0bb1-5acb-4332-aef4-e9f293a20a3f
- 定时：每天 08:00 Asia/Shanghai（cron expr: 0 8 * * *）
- 投递：Napcat -> private:3228996679（announce 模式），sessionTarget: isolated
- 状态：已创建，已触发一次手动运行并成功投递（run 状态 ok，已 deliver）。

4) 模型偏好（主人要求）
- 主人偏好：平聊使用 github-copilot/gpt-5-mini；在执行任务时自动切换到 github-copilot/gpt-5.3-codex（用户希望的 gpt-5.3-mini 未可用，已说明并用 gpt-5.3-codex 替代）。

5) ACP/Claude 状态
- 曾尝试 spawn ACP (agentId: claude) 以用 Claude Code 完成编码任务，但失败："ACP runtime backend is not configured"（需安装/启用 acpx 插件）。因此本次改为本地实现脚本并用 openclaw cron 注册。

6) 备注
- 该自动化已在当次测试中正常运行并将报告发送到 Napcat/QQ；后续若需用 Claude Code，请先配置 ACP 后端或允许使用对应 acp.agent。

Logged: 2026-03-23T22:04:00+08:00
Source: session_actions
Tags: automation, cron, hf-papers, model-preference, acp


2026-03-23T22:12:34+0800 已将模型切换规则写入 SOUL.md
- 规则要点：平聊使用 github-copilot/gpt-5-mini；任务时切换到 github-copilot/gpt-5.3-codex（若 github-copilot/gpt-5.3-mini 可用则优先）；切换前会通知，完成后恢复默认模型。
Logged: 2026-03-23T22:12:34+0800
Source: session_actions
Tags: preference, model-switch
